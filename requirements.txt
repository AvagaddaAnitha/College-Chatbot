# =============================================================
# requirements.txt
# These are all the Python packages our project needs.
# Streamlit Cloud reads this file automatically and installs them.
# Run locally with:  pip install -r requirements.txt
# =============================================================

streamlit==1.32.0
pandas==2.2.3
numpy==1.26.4
faiss-cpu==1.7.4
sentence-transformers==2.2.2
google-generativeai==0.3.2
python-dotenv==1.0.1
openpyxl==3.1.2
